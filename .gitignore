# dependencies
node_modules/

# next.js
.next/
out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# local tool config (not meant for the shared repo)
.claude/

# pnpm auto-generates this as a placeholder when prompting about build
# scripts (e.g. sharp). This is a single-package repo, not a workspace, and
# committing it breaks CI: pnpm aborts with "packages field missing or empty".
pnpm-workspace.yaml
